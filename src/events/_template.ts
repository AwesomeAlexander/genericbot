import { Discord, Client } from '../utils/exports';

/**
 * Function to be executed when the event is fired.
 * Parameters will vary by function, first parameter is the client.
 */
export default function(client: Client, ...args) {
	
}